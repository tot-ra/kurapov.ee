name: deploy
on:
  push:
    branches: main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: deploy via restart.sh
        working-directory: /www/kurapov.ee/
        run: chmod +x /www/kurapov.ee/restart.sh && /www/kurapov.ee/restart.sh
